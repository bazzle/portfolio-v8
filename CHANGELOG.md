# Changelog

## Unreleased

- Add design tokens from Figma: 54 primitives, 18 themed colours across light and dark, and the nine-character type scale.
- Wire up the `src/ui` structure: add `sass`, point the root layout at `main.scss`, and name component files after their component.

- Scaffold Next.js 16 App Router project with TypeScript, ESLint and Yarn Berry; stripped to a blank slate with Satoshi font files staged in `src/assets/fonts/`.
